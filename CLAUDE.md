# CLAUDE.md

Agent guidance for working in this repo lives in [`AGENTS.md`](./AGENTS.md).

Read it first — it covers build/test/run commands, project structure, the Claude-CLI-as-model-backend constraint, the JSON storage layer, config defaults, and code style. Keep `AGENTS.md` as the single source; this file is just the Claude Code entry point.
